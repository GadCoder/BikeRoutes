# Shared
APP_ENV=development

# Database (docker compose)
POSTGRES_USER=bikeroutes
POSTGRES_PASSWORD=bikeroutes
POSTGRES_DB=bikeroutes
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
DATABASE_URL=postgresql+asyncpg://bikeroutes:bikeroutes@localhost:5432/bikeroutes
CORS_ORIGINS=http://localhost:5173
JWT_SECRET=dev-insecure-change-me
# Comma-separated Google OAuth client IDs accepted by /auth/google.
# Include all clients that can mint ID tokens for this backend (web + mobile).
GOOGLE_CLIENT_IDS=
GOOGLE_JWKS_URL=https://www.googleapis.com/oauth2/v3/certs
GOOGLE_ISSUERS=accounts.google.com,https://accounts.google.com

# Web (Vite)
VITE_GOOGLE_CLIENT_ID=

# Mobile (Expo)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_EXPO_CLIENT_ID=

# Tiles (Martin)
MARTIN_PORT=3000
